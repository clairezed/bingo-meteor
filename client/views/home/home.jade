template(name="home")
  .row
    .col-md-8.col-md-offset-2
      h1 Fancy a sweet bingo party ?
      p
        | Everyone liiiiikes bingo. Here, you can create your own Bingo grid with the content you want,
        | or play to a Bingo game created by someone else.
      p.text-center
        a.play-bingo.btn.btn-primary.btn-lg(href="{{#if currentUser}}{{pathFor 'createGrid'}}{{else}}{{pathFor 'authenticate'}}{{/if}}")
          | Create a bingo game
  +latestGridsList

template(name="latestGridsList")
  .row
    .col-md-8.col-md-offset-2
      h3 Latest Bingos
      ul.row
        each readyGrids
          +gridThumbnail
        else
          li
            | No bingos for now, sorry !

